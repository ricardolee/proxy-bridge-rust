[package]
name = "proxy-bridge-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cross-platform CLI argument parsing for ProxyBridge"

[dependencies]
proxy-bridge-core = { path = "../proxy-bridge-core" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
